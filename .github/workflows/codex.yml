name: Codex

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  pull_request_review:
    types: [submitted]
  workflow_dispatch:
    inputs:
      pr_number:
        description: Pull request number. Defaults to the most recently updated open PR.
        required: false
        type: string
      instructions:
        description: Optional instructions for the ship workflow.
        required: false
        type: string
      autoreview_model:
        description: Optional Codex model for autoreview.
        required: false
        type: string
      autoreview_thinking:
        description: Optional Codex reasoning effort for autoreview.
        required: false
        type: string
      autoreview_parallel_tests:
        description: Optional test command to run in parallel with autoreview.
        required: false
        type: string

jobs:
  resolve:
    if: |
      (github.event_name == 'issue_comment' && github.event.issue.pull_request && (contains(github.event.comment.body, '@codex') || contains(github.event.comment.body, '@autoreview')) && contains(fromJSON('["OWNER", "MEMBER", "COLLABORATOR"]'), github.event.comment.author_association)) ||
      (github.event_name == 'pull_request_review_comment' && (contains(github.event.comment.body, '@codex') || contains(github.event.comment.body, '@autoreview')) && contains(fromJSON('["OWNER", "MEMBER", "COLLABORATOR"]'), github.event.comment.author_association)) ||
      (github.event_name == 'pull_request_review' && (contains(github.event.review.body, '@codex') || contains(github.event.review.body, '@autoreview')) && contains(fromJSON('["OWNER", "MEMBER", "COLLABORATOR"]'), github.event.review.author_association)) ||
      github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
      issues: read
    outputs:
      pr_number: ${{ steps.pr.outputs.number }}
      base_ref: ${{ steps.pr.outputs.base_ref }}
      base_sha: ${{ steps.pr.outputs.base_sha }}
      head_ref: ${{ steps.pr.outputs.head_ref }}
      head_sha: ${{ steps.pr.outputs.head_sha }}
      head_repo: ${{ steps.pr.outputs.head_repo }}
      title: ${{ steps.pr.outputs.title }}
      body: ${{ steps.pr.outputs.body }}
      instructions: ${{ steps.pr.outputs.instructions }}
    steps:
      - name: Resolve pull request context
        id: pr
        uses: actions/github-script@v7
        with:
          github-token: ${{ github.token }}
          script: |
            const { owner, repo } = context.repo;
            let pull;
            let instructions = core.getInput('instructions') || 'Manual Codex workflow run.';

            if (context.eventName === 'issue_comment') {
              const issue = context.payload.issue;
              if (!issue.pull_request) {
                core.setFailed('Codex can only run from pull request comments.');
                return;
              }
              const response = await github.rest.pulls.get({
                owner,
                repo,
                pull_number: issue.number,
              });
              pull = response.data;
              instructions = context.payload.comment.body;
            } else if (context.eventName === 'pull_request_review_comment') {
              pull = context.payload.pull_request;
              instructions = context.payload.comment.body;
            } else if (context.eventName === 'pull_request_review') {
              pull = context.payload.pull_request;
              instructions = context.payload.review.body || '';
            } else {
              const manualPr = core.getInput('pr_number');
              if (manualPr) {
                const response = await github.rest.pulls.get({
                  owner,
                  repo,
                  pull_number: Number(manualPr),
                });
                pull = response.data;
              } else {
                const pulls = await github.rest.pulls.list({
                  owner,
                  repo,
                  state: 'open',
                  per_page: 1,
                  sort: 'updated',
                  direction: 'desc',
                });
                if (pulls.data.length === 0) {
                  core.setFailed('No open pull requests found for manual Codex run.');
                  return;
                }
                pull = pulls.data[0];
              }
            }

            core.setOutput('number', String(pull.number));
            core.setOutput('base_ref', pull.base.ref);
            core.setOutput('base_sha', pull.base.sha);
            core.setOutput('head_ref', pull.head.ref);
            core.setOutput('head_sha', pull.head.sha);
            core.setOutput('head_repo', pull.head.repo.full_name);
            core.setOutput('title', pull.title || '');
            core.setOutput('body', pull.body || '');
            core.setOutput('instructions', instructions);

  ship:
    needs: resolve
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write
      actions: read
    outputs:
      final_message: ${{ steps.run_ship.outputs['final-message'] || steps.run_autoreview.outputs['final-message'] }}
    steps:
      - name: Checkout pull request branch
        uses: actions/checkout@v4
        with:
          repository: ${{ needs.resolve.outputs.head_repo }}
          ref: ${{ needs.resolve.outputs.head_ref }}
          fetch-depth: 0

      - name: Pre-fetch base ref for autoreview
        env:
          PR_BASE_REF: ${{ needs.resolve.outputs.base_ref }}
        run: |
          git fetch --no-tags "https://github.com/${{ github.repository }}.git" \
            "+refs/heads/$PR_BASE_REF:refs/remotes/origin/$PR_BASE_REF"

      - name: Install Codex CLI for autoreview
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install autoreview engine
        run: npm install -g @openai/codex

      - name: Run autoreview gate
        id: run_autoreview
        shell: bash
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          PR_BASE_REF: ${{ needs.resolve.outputs.base_ref }}
          AUTOREVIEW_MODEL: ${{ github.event.inputs.autoreview_model }}
          AUTOREVIEW_THINKING: ${{ github.event.inputs.autoreview_thinking }}
          AUTOREVIEW_PARALLEL_TESTS: ${{ github.event.inputs.autoreview_parallel_tests }}
        run: |
          set +e
          output_file="$RUNNER_TEMP/autoreview-output.txt"
          review_args=(
            catalog/portable/autoreview/scripts/autoreview
            --mode branch
            --base "origin/$PR_BASE_REF"
            --engine codex
            --stream-engine-output
          )
          if [ -n "$AUTOREVIEW_MODEL" ]; then
            review_args+=(--model "$AUTOREVIEW_MODEL")
          fi
          if [ -n "$AUTOREVIEW_THINKING" ]; then
            review_args+=(--thinking "$AUTOREVIEW_THINKING")
          fi
          if [ -n "$AUTOREVIEW_PARALLEL_TESTS" ]; then
            review_args+=(--parallel-tests "$AUTOREVIEW_PARALLEL_TESTS")
          fi

          "${review_args[@]}" 2>&1 | tee "$output_file"
          status=${PIPESTATUS[0]}

          {
            echo "status=$status"
            echo "final-message<<AUTOREVIEW_RESULT"
            python3 - "$status" "$output_file" <<'PY'
          import sys
          from pathlib import Path

          status = int(sys.argv[1])
          text = Path(sys.argv[2]).read_text(errors="replace")
          if len(text) > 60000:
              text = text[-60000:]
              text = "[truncated to last 60000 characters]\n" + text
          outcome = "passed" if status == 0 else "failed"
          print(f"### Autoreview gate {outcome}")
          print()
          print(f"Exit status: `{status}`")
          print()
          print("````text")
          print(text.rstrip())
          print("````")
          PY
            echo "AUTOREVIEW_RESULT"
          } >> "$GITHUB_OUTPUT"

      - name: Stop when autoreview reports findings
        if: steps.run_autoreview.outputs.status != '0'
        run: exit ${{ steps.run_autoreview.outputs.status }}

      - name: Run Codex ship workflow
        id: run_ship
        uses: openai/codex-action@v1
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        with:
          openai-api-key: ${{ secrets.OPENAI_API_KEY }}
          prompt: |
            You were invoked from GitHub Actions after the autoreview gate passed.

            Follow the repository skill workflows exactly:
            1. Read catalog/portable/ship/SKILL.md.
            2. Read catalog/portable/autoreview/SKILL.md.
            3. Ship the current pull request branch according to $ship.
            4. If you make any code changes before pushing, rerun:
               catalog/portable/autoreview/scripts/autoreview --mode branch --base origin/${{ needs.resolve.outputs.base_ref }} --engine codex --stream-engine-output
               and stop instead of pushing if it reports accepted/actionable findings.

            Repository: ${{ github.repository }}
            Pull request: #${{ needs.resolve.outputs.pr_number }}
            Title: ${{ needs.resolve.outputs.title }}

            Pull request body:
            ----
            ${{ needs.resolve.outputs.body }}

            Comment or manual instructions:
            ----
            ${{ needs.resolve.outputs.instructions }}

  post_feedback:
    runs-on: ubuntu-latest
    needs:
      - resolve
      - ship
    if: always() && needs.resolve.outputs.pr_number != '' && needs.ship.outputs.final_message != ''
    permissions:
      issues: write
      pull-requests: write
    steps:
      - name: Report Codex response
        uses: actions/github-script@v7
        env:
          FINAL_MESSAGE: ${{ needs.ship.outputs.final_message }}
          PR_NUMBER: ${{ needs.resolve.outputs.pr_number }}
        with:
          github-token: ${{ github.token }}
          script: |
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: Number(process.env.PR_NUMBER),
              body: process.env.FINAL_MESSAGE,
            });
