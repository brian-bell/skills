Commit, push, and open a PR for the current branch. Follow these steps:

1. Run `/commit` to commit the current changeset
2. Push the current branch to the remote
3. If the current branch has NO open PR, create one with `gh pr create` and write a detailed description covering all changes on the branch
4. If the current branch already has an open PR, do NOT update the PR description unless explicitly asked to
