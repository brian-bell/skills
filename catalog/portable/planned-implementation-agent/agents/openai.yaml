interface:
  display_name: "Planned Implementation Agent"
  short_description: "Plan review, TDD work, and 2 implementation loops"
  default_prompt: "Use $planned-implementation-agent with $tdd and $review-loop to plan, review, and delegate this implementation task."
